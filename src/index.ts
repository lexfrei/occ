/**
 * OCC — OpenClaw-Claude Connector
 *
 * Bridge between Claude Code Channels and OpenClaw Gateway.
 * Runs as a Claude Code Channel MCP server that connects to
 * OpenClaw Gateway as a WebSocket operator client.
 */

export function main(): void {
  console.error("[occ] starting...");
}

main();
